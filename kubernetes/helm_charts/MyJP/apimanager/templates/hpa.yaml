{{- if .Values.apimanagerConfig.autoscaleEnabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}
  minReplicas: {{ .Values.apimanagerConfig.minReplicas }}
  maxReplicas: {{ .Values.apimanagerConfig.maxReplicas }}
  metrics:
    {{- if .Values.apimanagerConfig.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.apimanagerConfig.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.apimanagerConfig.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.apimanagerConfig.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
